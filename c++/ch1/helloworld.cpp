#include <iostream> // this is the name of the input and output cpp library use the include to import the library

int main(void) // The main functions that runs everything in your program
{
  std::cout << "Hello World" << std::endl; // outputs hellowrld
                                           // cout is character output
                                           // << insertion operation send the "helloworld" characters to the cout object
                                           // std::endl object to insert a newline character after the statement has printed
  return 0;
}
